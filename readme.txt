To run the JUnit test cases
./gradlew test

To run AndroidTest cases
./gradlew connectedAndroidTest or ./gradlew cAT

If you are executing these tests on Mac then you might have to give executaion permission on gradlew
chmod +x gradlew